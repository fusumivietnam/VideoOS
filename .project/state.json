{
  "project": "VideoOS",
  "updatedAt": "2026-09-17",
  "milestone": "M4",
  "milestoneName": "Multi-network publishing",
  "status": "active",
  "currentFocus": [
    "VID-12 OAuth onboarding/refresh and manual live-smoke implementation is merged; complete explicit real YouTube private-upload/reconciliation verification before treating publishing as launch-verified",
    "VID-23 runtime config boundary and fake publisher are in progress so the approved publish loop can be exercised end to end without external credentials while production remains fail-closed",
    "finish MVP end-to-end behavior before production credential/config rollout; add config contracts/defaults only when the active vertical slice requires them",
    "real publish launch remains gated by VID-12; do not use fake publishing as a production fallback or start a second provider before YouTube verification"
  ],
  "completedMilestones": ["M0", "M1", "M2", "M3"],
  "completedCapabilities": [
    "PostgreSQL membership, asset metadata, durable queue and outbox adapters",
    "PostgreSQL runtime composition and durable publisher idempotency storage",
    "atomic terminal queue settlement plus lifecycle outbox persistence",
    "repository-owned canonical Project Brain state",
    "filesystem and S3-compatible object storage behind stable ports",
    "path-gated PostgreSQL integration in the single CI quality job",
    "durable runtime health, backup and recovery guidance",
    "sandboxed deterministic FFmpeg execution with bounded process/resource behavior",
    "ffprobe normalization and idempotent derived-asset persistence with source/job/transform/executor lineage",
    "deterministic social media presets with preset-aware reproducible lineage",
    "project-scoped subtitle staging and sandboxed subtitle burn-in",
    "optional local-node media lease execution with generic CPU/GPU requirements and queue-authoritative retries",
    "deterministic JPEG/PNG thumbnail and representative-frame extraction with image asset lineage",
    "retry-safe YouTube publishing adapter with trusted object-store asset loading, resumable upload, durable attempt journaling, normalized receipts, and exact reconciliation",
    "server-side YouTube OAuth onboarding/refresh lifecycle with explicit opt-in live smoke tooling and bounded secret-safe diagnostics",
    "explicit project-authorized publish approval with audit-preserved internal job payload plus pre-enqueue asset and schedule validation",
    "repository-owned controlled launch readiness command and runbook",
    "dependency-free read-only web launch cockpit deriving readiness only from canonical repository state",
    "localhost-only non-root read-only controlled-alpha container packaging with simple operator lifecycle commands",
    "optional protected alpha access using bounded HMAC-signed HttpOnly sessions with fail-closed non-loopback binding",
    "principal-scoped project membership discovery with in-memory/PostgreSQL adapters and project-authorized product read models for projects, assets, and job status",
    "read-only authenticated product BFF with per-user access-code exchange, signed principal sessions, and project-scoped project/assets/job endpoints",
    "same-origin user-facing product read UI for login, project memberships, project assets and job lookup behind the authenticated BFF",
    "bounded authenticated media-job creation with strict transform validation plus fixed 720p proxy action in the product UI",
    "bounded automatic media-job tracking with terminal-state handling and authorized asset refresh in the product UI",
    "bounded YouTube publish preflight reusing canonical publish validation and explicitly preventing enqueue before provider verification"
  ],
  "blockers": [
    "VID-12 real YouTube private-upload/reconciliation smoke has not yet been recorded as verified",
    "controlled end-user publish enqueue remains locked until the first live YouTube path is verified or explicitly waived"
  ],
  "nextTasks": ["VID-23", "VID-12", "VID-5"],
  "canonicalSources": {
    "roadmap": "docs/roadmap.md",
    "tasks": "backlog/tasks",
    "decisions": "backlog/decisions",
    "architecture": "docs/architecture",
    "agentGuide": "AGENTS.md"
  },
  "derivedKnowledge": {
    "understandAnything": ".ua",
    "sourceOfTruth": false
  }
}
