{
  "include": [
    "apps/**/*.{ts,tsx,js,jsx}",
    "services/**/*.{ts,tsx,js,jsx,go,py}",
    "packages/**/*.{ts,tsx,js,jsx,go,py,json,yaml,yml}",
    "tooling/**/*.{ts,js,mjs,cjs,py,sh}",
    ".github/workflows/**/*.{yaml,yml}"
  ],
  "exclude": [
    "**/node_modules/**",
    "**/dist/**",
    "**/build/**",
    "**/coverage/**",
    "**/.next/**",
    "**/generated/**",
    "**/*.snap",
    "**/*.lock",
    "**/fixtures/binary/**"
  ],
  "rules": [
    {
      "path": "packages/contracts/**/*",
      "rule": "Treat this as a public compatibility boundary. Check backward compatibility, schema/version evolution, optional-vs-required fields, serialization stability, idempotency fields, UTC timestamp semantics, and accidental leakage of secrets or provider-specific implementation details. Flag breaking changes without an explicit migration/versioning strategy."
    },
    {
      "path": "packages/event-fabric/**/*",
      "rule": "Focus on event delivery correctness: at-least-once semantics, duplicate handling, ordering assumptions, transactional outbox safety, correlation/project scoping, replayability, poison events, backpressure, and provider-neutral boundaries."
    },
    {
      "path": "packages/job-queue/**/*",
      "rule": "Focus on durable queue semantics: lease ownership and expiry, retry/backoff, dead-letter handling, idempotent enqueue, clock/time comparisons, crash recovery, duplicate workers, starvation and provider-neutral behavior."
    },
    {
      "path": "packages/storage/**/*",
      "rule": "Treat storage as a trust boundary. Check project isolation, path/key traversal, untrusted file metadata, checksum/integrity handling, signed URL scope/expiry, deletion semantics, overwrite behavior and provider neutrality."
    },
    {
      "path": "packages/identity/**/*",
      "rule": "Treat authorization as correctness-critical. Check deny-by-default behavior, project/tenant isolation, capability escalation, role mutation, confused-deputy risks, service/node principals, missing membership checks and authorization decisions made from untrusted client input."
    },
    {
      "path": "packages/node-protocol/**/*",
      "rule": "Review local-node protocol as a remote execution security boundary. Check node authentication, project scoping, capability negotiation, lease ownership/expiry, replay attacks, task/result correlation, untrusted editor commands, version compatibility and secret exposure."
    },
    {
      "path": "services/api/**/*",
      "rule": "Focus on request authorization and orchestration boundaries: authenticate principals, authorize every project-scoped operation, validate resource ownership, avoid direct provider side effects, make enqueue/idempotency behavior explicit, normalize errors and never trust client-supplied project/resource relationships."
    },
    {
      "path": "services/orchestrator/**/*",
      "rule": "Focus on durable workflow correctness: deterministic state transitions, retries/backoff, idempotency, cancellation, concurrency, race conditions, timeouts, partial failure, duplicate delivery, recovery after process crash, and separation between deterministic control logic and non-deterministic AI reasoning."
    },
    {
      "path": "services/publisher/**/*",
      "rule": "Focus on external side effects and multi-network publishing: idempotency keys, duplicate publish prevention, rate limits, remote status reconciliation, auth/token handling, scheduling/time zones, retry classification, partial success, platform-specific policy isolation, and canonical-contract-to-adapter translation."
    },
    {
      "path": "services/ai-gateway/**/*",
      "rule": "Focus on provider isolation, prompt/tool injection boundaries, model/tool authorization, timeout/retry/circuit-breaker behavior, token/cost controls, redaction, structured output validation, deterministic fallbacks, observability, and prevention of provider-specific types leaking into domain code."
    },
    {
      "path": "services/media-worker/**/*",
      "rule": "Focus on untrusted media/file handling, command injection, path traversal, temporary-file cleanup, resource limits, cancellation, deterministic outputs, codec/tool failures, GPU/CPU exhaustion, resumability and artifact integrity."
    },
    {
      "path": "services/analytics/**/*",
      "rule": "Review analytics ingestion for project scoping, event validation, PII/secrets minimization, duplicate/replay handling, stable schemas, cardinality/cost risks, retention assumptions and separation from transactional source-of-truth state."
    },
    {
      "path": "packages/adapters/**/*",
      "rule": "Check that adapters remain replaceable and do not contaminate the core. Verify canonical input/output translation, capability detection, error normalization, retryability classification, version pinning assumptions and contract tests for each external system."
    },
    {
      "path": "apps/web/**/*",
      "rule": "Focus on tenant isolation, authorization, unsafe client trust, XSS/CSRF, upload safety, optimistic update consistency, accessibility, performance regressions and clear handling of long-running asynchronous workflow states."
    },
    {
      "path": "infra/**/*",
      "rule": "Treat infrastructure as security-sensitive. Check least privilege, secret exposure, public ingress, network boundaries, persistence/backups, rollout/rollback safety, image/version pinning, resource limits, observability and disaster recovery assumptions."
    },
    {
      "path": ".github/workflows/**/*.{yaml,yml}",
      "rule": "Review CI/CD for supply-chain security and correctness: pin third-party actions where practical, minimize permissions, protect secrets on forked PRs, avoid executing untrusted PR code with privileged tokens, use concurrency intentionally, cache safely, and keep quality/security gates reproducible."
    }
  ]
}
